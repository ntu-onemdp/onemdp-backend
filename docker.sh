# Simple shell script to create and run container
docker rm -f onemdp-dev-1 || true && DOCKER_BUILDKIT=1 docker build -f Dockerfile.dev  -t onemdp-dev-1 . && docker run --env-file config/.env.dev -it -p 8080:8080 --name onemdp-dev-1 onemdp-dev-1